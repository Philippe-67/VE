@model VE.Models.Interventions

@*@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Interventions</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
*@@{
    ViewData["Title"] = "Sélectionner un type d'intervention";
}

<h1>Sélectionner un type d'intervention</h1>

<form asp-action="HandleInterventionType">
    <div class="form-group">
        <label for="interventionType">Type d'intervention :</label>
        <select class="form-control" id="interventionType" name="interventionType">
            @foreach (var interventionType in ViewBag.InterventionTypes)
            {
                <option value="@interventionType.Name">@interventionType.Name</option>
            }
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Valider</button>
</form>